module = {
    name: 'lower case',
    description: 'convert text to lower case',
    categories: ['string'],
    properties: [],
    run: function (text) {
        return text.toLowerCase();
    }
}
