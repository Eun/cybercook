module = {
    name: 'lower case',
    description: 'convert text to lower case',
    categories: ['string'],
    properties: [],
    run: async function (text) {
        return text.toLowerCase();
    }
}
