module = {
    name: 'upper case',
    description: 'convert input to upper case',
    categories: ['string'],
    properties: [],
    run: function (text) {
        return text.toUpperCase();
    }
}
