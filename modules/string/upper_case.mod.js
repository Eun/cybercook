module = {
    name: 'upper case',
    description: 'convert text to upper case',
    categories: ['string'],
    properties: [],
    run: function (text) {
        return text.toUpperCase();
    }
}
